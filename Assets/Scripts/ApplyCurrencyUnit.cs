using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ApplyCurrencyUnit : MonoBehaviour
{
    //gameObject에서 해당 텍스트값 가져와서 해당 텍스트를 뒤에서부터 세 자리일때마다 잘라서 출력.
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
